#!/usr/bin/env node
var debug = require('debug')('pwrvid');
var app = require('../app');

app.server.listen(app.app.get('port'), function() {
  debug('Express server listening on port ' + app.server.address().port);
});